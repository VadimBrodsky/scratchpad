# Migration Methods: Table Migration


!! Create table:

```ruby
create_table(table, options) do |t|
  ...columns...
end
```

!! Drop Table:

```ruby
drop_table(table)
```

!! Rename Table:

```ruby
rename_table(table, new_name)
```