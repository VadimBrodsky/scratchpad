---
layout: null
---
{% for post in site.posts %}
[
	{
		"id": "{{ post.path }}",
		"title": "{{ post.title }}",
		"content": "{{ post.content | xml_escape }}",
		"category": "{{ post.category }}",
		"link": "{{ site.url }}{{ post.url }}",
		"date": "{{ post.date }}"
	}
]
{% endfor %}